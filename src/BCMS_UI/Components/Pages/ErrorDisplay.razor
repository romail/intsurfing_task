@if (ShowError)
{
<div class="alert alert-danger" role="alert">
    @Message
</div>
}

@code {
    [Parameter]
    public string Message { get; set; }
    [Parameter]
    public bool ShowError { get; set; }
}